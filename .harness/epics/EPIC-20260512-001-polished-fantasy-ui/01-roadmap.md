# Epic Roadmap

## Run 1: UI Foundation And Effects

- Tạo layout fantasy game screen trong existing component structure.
- Thêm component token/core/effects nhẹ nếu cần.
- Thêm event metadata nhẹ trong `GameState` nếu cần để UI quan sát animation.
- Chạy test/build/smoke.

## Follow-Up Runs If Needed

- Browser polish fixes found during manual smoke.
- Additional tests for animation/event metadata if evaluator finds coverage gaps.
